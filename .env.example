# Copy to .env and fill in. .env is gitignored — never commit the real URL,
# anyone holding it can post to your channel.
DISCORD_WEBHOOK_URL=

# Optional: link included in each Discord message, pointing at the full list.
PAGE_URL=
